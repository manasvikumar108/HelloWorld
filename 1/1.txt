git config --global user.name "Manasvi"
git config --global user.email "youremail@yourdomain.com"

cd dir location  [ cd d: ] [ cd 6/winterPep/a]  

create file index.html

git init

git status

git add index.html

create 2 extra files

git add --all

git commit -m "first release"

git status --short                     command will show the current state of your Git working directory and staging area. 			        	It will show which files have been modified, added, or deleted, as well as which 					files are untracked.

git commit -a -m "Second Release"

git branch helloworldImages

git branch

git checkout helloworldImages




